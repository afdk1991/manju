<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 网络：内容接口与 OTA 下载均为 HTTPS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      Android 8.0+ 通过 PackageInstaller 安装未知来源 APK 必须声明此权限。
      仅声明还不够：用户需在系统设置中打开"允许来自此来源的应用"。
      对应逻辑见 UpdaterModule.kt 的 canInstallPackages / openInstallPermissionSettings。
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          FileProvider：将私有缓存目录中的下载 APK 以 content:// URI 暴露给
          系统的 PackageInstaller。authority 必须为 ${applicationId}.fileprovider，
          与 file_paths.xml 中的定义保持一致。
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
