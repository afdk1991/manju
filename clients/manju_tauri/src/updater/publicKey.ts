// ============================================================================
// 内置 Ed25519 公钥（自动更新链路的信任根）
//
// 与 Flutter 端 `kOtaPublicKeyBytes`、RN / ArkTS 端同源，由团队统一签发，
// 编译期内置于客户端，用于校验 OTA 安装包的 Ed25519 签名。
//
// ⚠️ 严禁在本仓库提交任何对应的私钥。私钥仅存于 CI 签名机 / 密钥库。
//    若需轮换密钥，务必先发布新版（内置新公钥）再撤销旧私钥，避免已发布
//    版本无法验签。
// ============================================================================

/**
 * 内置 Ed25519 公钥（32 字节，Base64 编码）。
 * 真实值（与 spec/ota-protocol.v1.md 一致，四端共用）：
 */
export const PUBLIC_KEY_BASE64 = "f2obZdLFwhWJtuA/u/VW/EB1jAZ/UiZF/eEbxFJ0a3c=";

/**
 * 是否强制要求签名校验通过才放行。
 * - 生产环境必须保持 `true`：缺少或签名不符的安装包一律拒绝。
 * - 调试期（例如本地用未签名产物自测）可临时置 `false`，仅依赖 SHA256 校验，
 *   但发版前必须改回 `true`。
 */
export const REQUIRE_SIGNATURE = true;
