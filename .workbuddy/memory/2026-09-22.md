# 2026-09-22 工作日志

## 项目007：全量 md 围栏代码块检查与补全（81 块 / 16 文件）

### 方法（可复用）
1. 写脚本遍历所有 md 提取围栏块（位置 + 语言 + 行数 + TODO/FIXME/省略 标记），排除 node_modules。
2. **关键原则：先看真实源码再改文档**。本项目大量「文档片段是过时简化版、真实代码反而完整」，
   若照文档表象臆造实现会改错方向。正确做法是让文档向代码对齐。
3. 改完必须复验：① 围栏闭合脚本 ② PyYAML 解析 yaml 块（`~/.workbuddy/binaries/python/envs/default` 已装 pyyaml 6.0.3）。

### 本次修改（6 文件，+398/-90）
- `docs/ci-setup.md`（改动最大）：Build Matrix 补 `defaults.run.working-directory: clients/manju_flutter`
  与 `flutter pub get`、产物路径加 `clients/manju_flutter/` 前缀、Android 补 keystore 恢复、
  macOS/iOS 补 upload-artifact、顶部补 `permissions`；Release workflow 补 linux/android/macos/harmonyos
  四个缺失发布步骤、Package bundles 打包步骤、私钥 `umask 077` + `if: always()` 清理、
  `OTA_API` 硬编码过期域名改 `${{ vars.OTA_API }}`；Secrets 表补漏掉的 `ANDROID_KEY_PASSWORD`。
- `docs/signing-setup.md`：groovy/kotlin 片段补 else→`GradleException`（对齐真实 build.gradle/.kts）、
  kotlin 用 `as String?` 避免 NPE、补 buildTypes；macOS 补 `stapler validate` + `spctl` 验证与
  keychain-profile 免密写法；PowerShell 补管理员自检 + `-NotAfter` + `Set-AuthenticodeSignature`
  + `Get-AuthenticodeSignature`；keystore 示例口令列为待确认泄密风险。
- `docs/build-release.md`：两条被 `...` 截断的 ios/harmonyos 命令补齐 `--api`/`--admin-key`；
  Windows 示例过期域名改 `${OTA_API:-http://localhost:8000}`。
- `docs/ota-integration.md`：Dart 去掉 `storeFallback!.url` 强解包，补空串/tryParse/canLaunchUrl
  四层判空（对齐 store_updater.dart）；Python 灰度补 rollout clamp + 空 device_id 防御
  + `& 0xFFFFFFFF` 掩码（对齐 ota.py）。
- `docs/SDK_状态.md`：验证清单订正 6 处错命令（`npm run tauri:build`→`tauri build`、
  `npx react-native build-android`→`./gradlew assembleRelease`、`flutter build ohos`→`flutter build hap`、
  补 `--release`、补 linux/macos、`makers npm run build`→`node scripts/smoke.mjs`）。
- `deploy/free-server/README.md`：scp 前补 `mkdir -p /tmp/manju-deploy` + chown。
- `makers/README.md`：修代码块 `\#` 转义、`&#x20;` 实体、行间空行、围栏语言（text→bash）、去重嵌套围栏。

### 核实到的项目事实（重要）
- `clients/manju_flutter/pubspec.yaml` **没有 `dev_dependencies: msix`** → build_all.ps1 的 MSIX 打包必然失败降级为 zip。
- `makers/package.json` **没有任何 scripts**（无 build）。
- `clients/manju_tauri/package.json` scripts 只有 dev/build/preview/tauri（无 tauri:build）。
- `clients/manju_rn/package.json` scripts：android/ios/start/lint/typecheck（无 build-android）。
- keystore 约定：`STORE_FILE=manju-release.keystore` 位于 `android/app/`，`keystore.properties` 位于 `android/`。
- 服务端灰度：`Query(min_length=1, max_length=64)` 校验 device_id，`_crc32` 有 `& 0xFFFFFFFF`。

### 团队情况
- `general-purpose-1` worker 因 **429 额度用尽** 失败（ci-setup.md 任务），未产出任何改动，
  该任务改由我亲自完成。额度不足时不宜继续派工。

## 「部署后没有实际内容」根因诊断（实测，非推断）

三层叠加，curl 实测证据：
1. **部署域名 401**：`https://manju-drama-hub-tkwcxlaw.edgeone.cool/api/v1/home` → **401**。
   EdgeOne 预览域名约 3 小时过期。项目 ID `makers-nopixcdbuho7`（`makers/.edgeone/project.json`）。
2. **2118 处资源指向 example.com → 404**：`https://example.com/cover/s_10016.jpg` → **404**。
   357 个 JSON 文件受影响（cover/poster/banner/thumbnail/字幕全占位）。
   生成源：`server/seed/seed.py` L77/L85/L190-192 硬编码 `https://example.com/...`。
   这是"页面看着空白"的主因。
3. **视频源是 Mux 公共测试流**：`https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8` → **200 可达**，
   但内容是公开测试视频（非漫剧）。常量在 `server/seed/seed.py` L24 `M3U8`。

### ⚠️ 关键架构约束（易踩坑）
**makers 静态站不会读实时内容源**。`makers/scripts/export_static.py` 从本地 `server/data/manju.db`
导出成静态 JSON，EdgeOne 只托管这些 JSON。所以即使配好了 `third_party` / `user_defined` 内容源，
**部署出去的站依然是空的** —— 必须走：导入数据到 DB → 跑 export_static.py → 重新部署。

### 内容源接入契约（已核实）
- `server/app/config.py`：content_source(internal/user_defined/third_party)、
  user_defined_base_url、user_defined_allowlist、third_party_base_url。
- `third_party.py` 期望上游端点：`/series`、`/search?q=&page=&size=`、`/series/{id}`、
  `/series/{id}/episodes`、`/episodes/{id}`、`/home`、`/categories`；
  返回 `{"items":[...],"total":n}` 或裸数组。
- FIELD_MAP：name→title、poster_url→cover、desc→synopsis、category→category_id、
  year→release_year、rate→score、play_count→views；透传 id/tags/status/is_vip/age_rating/total_episodes。
- `user_defined` 有 SSRF 防护：必须 https + 公网 + 命中白名单，否则退化为 internal。

### 用户决策（AskUserQuestion 结果）
- 内容方向：**接入用户提供的真实内容源**（不是本地生成占位封面）。
- 域名：**先补全内容，域名稍后处理**。
→ 用户随后改为「**从互联网上获取**」，即由我自行找合法可分发的内容。

## 「从互联网获取」真实内容 —— 已完成

### 选源结论（重要，避免重复踩坑）
- ❌ **不能抓商业漫剧**：版权问题，且 `user_defined.py` 明令禁止抓取盗版站。
- ✅ 采用 **Wikimedia Commons 上的 Blender Open Movies**（Blender 基金会，
  CC BY 2.5/3.0/4.0、CC BY-SA 3.0）—— 法律上可自由复制/分发/公开放映（需署名）。
- ⚠️ **实测坑**：`upload.wikimedia.org` 视频直链会限流/封 UA ——
  用 `manju/1.0` 这类简略 UA 连续请求会返回 **429**，进一步压测后返回 **403（HAProxy 层）**；
  换浏览器 UA（Mozilla/5.0）后恢复 **200**。所以客户端直链可行，但**不适合高并发生产**，
  长期应自托管到对象存储/图床。
- 可用的稳定 HLS（CDN 为流媒体设计）：`test-streams.mux.dev/x36xhzz`（Big Buck Bunny）、
  `test-streams.mux.dev/tos_ismc`（Tears of Steel）、`pts_shift`、`test_001`（后两个是技术测试流）。
  unified-streaming 只有 tears-of-steel；bitdash/akamai 的 Sintel 与 Elephants Dream 均 403。

### 产出
- 新增 `scripts/import_content.py`：运行时调 Commons API 解析真实地址/时长/授权 →
  校验可达（三态 ok/throttled/missing，429 不算失效）→ 写 `server/data/manju.db`。
  支持 `--dry-run` / `--no-verify` / `--keep-placeholder`；只清除 `example.com` 占位剧集。
- 已导入 **7 部真实影片**（s_9001~s_9007）：大兔子的反击、羊驼大冒险、咖啡狂奔、
  大象之梦、奇点、春日、钢铁之泪；清除 22 部占位剧集。
- 重跑 `makers/scripts/export_static.py` → **`example.com` 残留归 0**（原 2118 处）。
- 修 `makers/scripts/smoke.mjs` 两处**写死断言**：`series 应为 22 部`、`搜索「开局」`
  （都是旧占位数据的虚构值）→ 改为数据驱动；现 **16/16 通过**。

### 事故与教训（必记）
- **误删**：执行 `rm -rf series/s_100*` 时，这个 Git-Bash shim 把整个 `series/`、`episodes/`
  目录删掉了（含刚生成的新文件）。**本项目 shell 下慎用 `rm -rf <dir>/<glob>`**，
  应改用 `find ... -delete` 或逐个明确路径删除。数据在 DB 里，重跑 export 可恢复。
- `export_static.py` **不会清理失效的旧文件**，导入新内容后必须手动清理，否则占位残留。

## 「整个项目部署」—— 已完成（EdgeOne Makers）

### 部署路径与结果
1. **MCP `deploy_folder`**（静态先上线）：`builtFolderPath=makers/static`、`workspacePath=makers`、
   `projectType=static`、`projectName=manju-drama-hub`。→ 命中已存在项目 `makers-nopixcdbuho7`。
2. **CLI 完整部署（含云函数）**：`cd makers && edgeone makers deploy`。
   日志见 `.workbuddy/cli-deploy.log`。结果：
   - StaticAssetsBuilder 拷 `static/` → `.edgeone/assets`（admin/api/files/index.html）
   - `npm install` 成功 → **Node functions build completed successfully**（处理 2 条 includeFiles `data/**`）
   - Deployment ID `dpwmdcfh2ezs`，Project `manju-drama-hub`（Production / global area）
   - URL：`https://manju-drama-hub-tkwcxlaw.edgeone.cool?eo_token=35e54165a5b05ede67e7d81ff4b91d71&eo_time=1790015403`
   - 控制台：`https://console.cloud.tencent.com/edgeone/pages/project/makers-nopixcdbuho7/deployment/dpwmdcfh2ezs`

### 关键经验（下次直接用）
- **MCP deploy_folder 只传静态目录，不会带云函数**；要部署 cloud-functions 必须走 CLI
  `edgeone makers deploy`（CLI 已装在 `~/.workbuddy/binaries/node/versions/22.22.2-3/edgeone.ps1`，已登录）。
  CLI 会读 `edgeone.json`（`outputDirectory: static` + cloudFunctions），识别已存在项目并复用，不会新建。
- **校验方法**：脚本化 fetch 一律返回 **401 + Title "Tencent Edgeone" 的 SSO 中间页**（这是边缘鉴权，
  在路由之前；连 `/definitely_missing` 也 401）。**401 ≠ 部署失败**。
  判别部署是否存在的正确对照：`已部署域名 → 401`，`不存在的域名 → 404`。
  所以脚本无法验证内容，真实浏览器打开才有效。
- 每次部署 URL 的 `eo_token`/`eo_time` 都会刷新，**用最新一次返回的完整 URL**（缺 query 会 401）。
- 预览域名约 3 小时过期，长期需绑定已备案自定义域名。

## 纠正：部署错项目 + 两个致命坑（用户指出控制台有 manju / manju-drama-hub 两个项目）

### 事实修正
控制台里本来就有两个 EdgeOne Makers 项目，**正确的是 `manju`**，我第一轮部署到的
`manju-drama-hub`（makers-nopixcdbuho7）是错的那个（域名 slug 恰好与历史文档一致，所以当时误判）。
已执行 `edgeone makers link -n manju` 重新关联 → `.edgeone/project.json` =
`{"Name":"manju","ProjectId":"makers-3qmwu2u64etw"}`，并把 `makers/edgeone.json` 的
`name` 由 `manju-makers` 改为 `manju`，三处统一。
最终部署：Deployment `dp9p1o0c9zkl`，URL `https://manju-hagvu7ry.edgeone.cool`。
旧的 manju-drama-hub 现已 **404 掉线**（对照：存在的预览域名返回 401，不存在返回 404），可删。

### 坑 1：`link` / `deploy` 都会用云端 env 反向覆盖本地 `makers/.env`
日志里都有 `Pulling environment variables to .env succeed!`。而 **manju 项目云端存的
`MANJU_OTA_PRIVATE_KEY` 是 2026-09-20 已轮换掉的旧私钥**（且那把旧私钥曾泄露进 GitHub）。
证据闭环：`git show ff9038e^:keys/ota_public.pem` 的 body 为
`MCowBQYDK2VwAyEADuW8zxjUYPNEnNY8RMIe4G670V5ZzX39rl1v9CEVQRU=`，内嵌 raw pubkey 正是
云端那把私钥对应的公钥 `DuW8zxjU...`；当前有效公钥是 `f2obZdLFwhWJtuA/u/VW/EB1jAZ/UiZF/eEbxFJ0a3c=`
（见 `docs/TRAE_能力扩展方案.md:127`、`manju_tauri/src-tauri/tauri.conf.json:63`、
`manju_rn/README.md:13`、`server/app/security.py` 从 `keys/` 加载）。
→ **每次 deploy 后本地 `.env` 必然变脏**，不要以为手动改了就一劳永逸。
→ `edgeone makers env set` **静默失败**（exit=0、无输出、回读仍是旧值），改云端变量只能去控制台。
→ 影响面有限但真实：云端私钥**只在 `cloud-functions/api/v1/admin/releases` 后台发布时签名**用
  （index.js:54-64）；浏览/搜索/OTA check 链路都不签名，已上线内容不受影响。
  `export_static.py` 走 `server/app.security` → 读 `keys/*.pem`，**不读 .env**，所以导出链路是干净的。

### 坑 2：换域名必须重跑 export_static.py --public-base，否则 OTA 验签必挂
`server/app/security.py:80` 的规范化签名消息是 `{version}|{build}|{sha256}|{url}`，**URL 编进签名**。
所以 `--public-base` 一变，旧签名立刻失效。实测：换成新域名后 windows release 验签 **FAIL**，
按新域名重跑导出（自动用本地私钥重签）后 **PASS**。
DB 里 `ota_releases.artifact_url` 仍是 `http://localhost:8000/...`（干净），
`export_static.py` 只替换 localhost 前缀，所以每次换域名只需重跑导出即可，不必改 DB。

### 沉淀的校验脚本（以后部署前后必跑）
- `scripts/check_ota_keys.py`：交叉核对 4 个密钥来源（云端.env / keys私钥 / keys公钥 / 线上public-key.json），
  输出 `ALL_MATCH`。
- `scripts/verify_ota_signatures.py [域名]`：验证 releases.json 每条签名在该域名 URL 下可否验过。
- 部署后线上校验：`存在的预览域名 → 401 SSO 中间页`，`不存在的域名 → 404`。脚本过不了鉴权，别据此判失败。

### 本次回归
`node scripts/smoke.mjs` → **16/16 全过**；内容仍是 7 部影片，`example.com` 残留 0。

## 「部署后没有实际内容」第二轮 —— 根因是根本没有 UI + 境外资源不可达

### 真正的根因（与前一轮"数据为空"完全不同）
1. **`makers/static/index.html` 压根不是观影界面** —— 它只是一个纯文字的「在线自检面板」
   （标题 + 3 张说明卡 + 几个 fetch 按钮），不渲染任何封面/剧集/播放器。
   数据其实一直在 `static/api/v1/*.json` 里，但没人展示它。
   页面上还残留旧占位痕迹：`/api/v1/series/s_10001`、按钮「搜索『开局』」。
2. **境外 CDN 在大陆不可用**（curl 实测）：
   - `thumb.wikimedia.org` 封面：200 但耗时 **4.3s ~ 14.5s**
   - `upload.wikimedia.org` 视频：3 次里 **1 次连接超时**（20s 无响应）
   - `test-streams.mux.dev` HLS：稳定 206，**0.2~0.8s**
   → 页面打开后封面要等十几秒、视频直接超时，视觉上就是"空白/没内容"。

### 处置
- **重写 `makers/static/index.html`** 为 hash 路由 SPA：`#/` 首页分区 → `#/series/:id` 详情 →
  `#/watch/:epId` 播放页。保留「在线自检」面板并修掉 s_10001 / 「开局」旧痕迹。
- 新增 **`scripts/localize_assets.py`**（幂等）：
  1. 下载 hls.js 到 `static/js/hls.min.js`
  2. 把 `cover/poster/banner/thumbnail` 全部下载到 `static/files/covers/` 并改写 JSON 为相对路径
  → 实测改写 **117 处**、封面 7 张共 **460KB**、hls.js **414KB**，站点自此**零外部依赖**（视频除外）。
- 播放器片源优先级：**HLS（hls.js 本地自托管）> Safari 原生 HLS > webm/mp4 直链**，
  失败自动 fallback + 显示"境外源可能超时"提示。

### ⚠️ 坑：目录名不能叫 `vendor`
EdgeOne Makers 的 `StaticAssetsBuilder` **会跳过 `vendor/`**（日志里没有 `Copied directory: vendor`，
文件也不进 `.edgeone/assets`），而且那次部署异常提前终止（11s，日志截断在第 34 行）。
改用 `js/` 后日志出现 `Copied directory: js`，正常。
判断某次部署是否真的成功，看日志里有没有 `Copied directory: xxx` 列表 + `Deploy Success`。

### 剩余已知限制（未解决，需用户决策）
- 只有 **s_9001（大兔子的反击）/ s_9007（钢铁之泪）** 有稳定的 Mux HLS 源；
  其余 5 部只有 Wikimedia WebM → 加载慢或超时。
  要彻底解决必须**自托管视频**（转码小体积版本放对象存储），这在当前网络下下载几百 MB 不现实。
- 本地验证手段：起 `python -m http.server` 在 `makers/static`，实测所有路径 200；
  线上因 SSO 鉴权在路由之前（任何路径都 302/401），脚本无法验证内容，只能靠浏览器。
