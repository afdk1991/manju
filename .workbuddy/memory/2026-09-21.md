# 2026-09-21 工作日志

## 项目007 SDK 依赖安装核验（manju 多端项目）
- 沙盒内可安装的 SDK 已全部到位并实测核验：
  - Python 3.13（managed venv）后端依赖：fastapi / uvicorn / sqlalchemy / pydantic-settings / cryptography / requests（合并覆盖 server/、scripts/、deploy/free-server/ 三处 requirements.txt）
  - Node 22（managed）三端 npm 依赖：manju_tauri(46)、manju_rn(409)、makers(1) 均安装成功
  - Flutter 3.47.5 + Dart SDK + manju_flutter `pub get` 完成（装于 `C:\Users\addk1\flutter`）
- 关键踩坑：Google Storage 在沙盒被限速 ~30–60KB/s（1.8GB 需数小时），改中国镜像 `storage.flutter-io.cn` + `PUB_HOSTED_URL=https://pub.flutter-io.cn` 后数分钟完成；解压时沙盒拦截个别测试源码文件写入（PermissionError），改为逐文件 extract 并跳过被拒文件（不影响构建）。
- 沙盒无法安装（需本机）：Rust/cargo（Tauri 编译）、MSVC Build Tools+Windows SDK（Tauri Win）、WebView2、Android SDK/NDK（RN/Flutter Android）、Xcode+CLT+CocoaPods（RN iOS/mac）、HarmonyOS SDK/DevEco+ohpm（RN/Flutter 鸿蒙）。GitHub 在沙盒超时，git clone 类工具链不可用。
- 产出文档：`docs/SDK_状态.md`（端→SDK 映射、已装清单、缺失 SDK 用途与用途、Win/macOS 完整安装方案、验证清单）。

## TRAE 能力扩展方案（全量 md 遍历）
- 完整读取 20 个自研 .md（排除 node_modules 下约 300+ 第三方 README/CHANGELOG），产出 `docs/TRAE_能力扩展方案.md`。
- **发现文档与现实的重大偏差**（待修）：`docs/SDK_状态.md` 与 `docs/blockers.md` §0 仍声称 Rust/MSVC/Android SDK/Gradle/Flutter 未安装、Linux 需外部机器 —— 实际已全部装于 `D:\toolchain`（rust/VS2022/android-sdk/gradle-8.9/flutter/wsl），且 `flutter build linux --release` 已产出 `clients/manju_flutter/build/linux/x64/release/bundle/manju`。这两份过期文档会让 AI/TRAE 误判「四端都不可构建」。
- 其它过期点：`clients/manju_rn/README.md` 写 RN 0.74（实际 package.json 为 **0.87.1**，react 19.2.3，已装 409 包）；`clients/manju_tauri/README.md` 写「无 Rust 工具链」（已装，但 `src-tauri/target/release/bundle/` 仍为空、尚未构建）；`dist/` 只有 2KB 假包 `fake-manju-1.1.0-win-x64.msix`。
- **本机 TRAE CN 实测**：`C:\Users\addk1\.trae-cn`；`plugin-config.json` 有 **103 个已启用插件**（含 github/gitlab/gitee/test-android-apps/build-macos-apps/context7/sentry/compliance-review/wecom/cloudflare/sealos/coderabbit/archcore 等）；`~/.trae-cn/skills/` 约 90 个技能（含 mcp-builder、react-native-skills、crossplatform-service-migration、electron、spec-to-implementation、security-best-practices、webapp-testing、local-screenshot-qa）；内置 4 个（TRAE-code-review / security-review / debugger / generate-mini-app）；**项目级 MCP 仅 2 个**（solo_agent_lite + pkulaw 法律库），无开发/部署类 MCP —— 这是最大缺口。
- **结论**：TRAE 短板不是缺插件而是缺「项目约束」。最高性价比三件事：① 建 `.trae/rules/` 写死 OTA 红线（iOS/鸿蒙禁静默安装、签名消息格式 `{version}|{build}|{sha256}|{url}`、禁下发 keys/*）；② 修上述过期文档；③ 用 `mcp-builder` 把 `scripts/`（ota_manifest.py / smoke_test.py / gen_keypair.py / gen_icons_all.py）包成 `manju-ota` MCP，其中 `ota_key_rotate` 强制同时回写 base64 与 bytes 两处，根除「轮换只改 base64 漏改 bytes」事故重演。
