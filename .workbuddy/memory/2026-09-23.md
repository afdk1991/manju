# 2026-09-23 工作日志

## 漫剧资源获取与集成（合规方案 + 脚手架）
- 任务：从互联网获取漫剧（AI 动态漫画短剧）资源并集成进 manju 项目，须符合平台 ToS。
- 合规结论：
  - ❌ 主流平台正片（红果/抖音精选/快手/腾讯/B站）禁止爬取（版权+ToS）；分发 SDK 需企业签约资质，非"下载"范畴。
  - ✅ 采用两类合规来源：① 自有 AI 生成（ImageGen/VideoGen，推荐）；② CC0 素材库（Pexels/Pixabay/Mixkit/Coverr/Vidlery/LottieFiles/OpenGameArt/unDraw/思源字体），须留存 provenance。
- 集成方式（贴合现有架构）：物理资源放 `server/data/artifacts/manju/<series_id>/...`，经 `main.py` 已挂载的 `/files` 静态路由对外；落到 `Series.cover/poster/banner` 与 `Episode.thumbnail/sources[VideoSource]/subtitles`，默认 `internal` 内容源即可返回。
- 交付物：
  - `docs/漫剧资源获取与集成方案.md`（四件事 + 合规清单）
  - `content/library/manifest.schema.json`、`content/library/sample_series.json`
  - `scripts/acquire_assets.py`（CC0 专用下载器，仅命中官方 API，自动写 provenance.jsonl；dry-run 不联网）
  - `scripts/seed_content_library.py`（manifest→DB 种子）
  - `server/data/artifacts/manju/.gitkeep`
- 验证：两脚本 py_compile 通过；acquire dry-run（无 key）仅打印计划、不联网。
- 状态：方案+脚手架完成，等待用户确认实际获取的来源与范围后再执行下载/生成。
