# 2026-09-20 项目007：全项目托管上线（EdgeOne Makers，免费）

## 做了什么
通读整个项目后，把项目部署到腾讯云 EdgeOne Makers（免费 Serverless）。

- 部署载体：`makers/`（此前已建好的 EdgeOne 无服务器适配工程）
- 部署工具：EdgeOne Makers 连接器 `deploy_folder`
- projectName：`manju-drama-hub`
- projectId：`makers-nopixcdbuho7`
- 区域：中国站（china）

线上地址（**带 eo_token，有效期约 3 小时，过期需在控制台重新预览**）：
https://manju-drama-hub-tkwcxlaw.edgeone.cool?eo_token=b948ff48f4a33023072a944fdf0f2c8b&eo_time=1789840241

## makers/ 架构（为什么这样设计）
EdgeOne Makers 的 Cloud Functions **文件系统不可持久化**，所以：
- SQLite 不部署，内容导出为**静态 JSON** 放 `static/api/v1/`，走 CDN 边缘加速
- `edgeone.json` 用 rewrites 把 `/api/v1/xxx` 映射到 `xxx.json`，
  客户端 API 路径与响应结构**完全不变**
- Cloud Functions 负责：OTA check / report、search、admin releases、user favorites/history
- `@edgeone/pages-blob`（免费 1GB）做持久化存储：OTA 上报、收藏、历史、admin 增量发布
- `data/**` 通过 `includeFiles` 打包进函数，供搜索与 OTA 检查读只读索引

**客户端零改动**：只需把 `MANJU_API_BASE` / `API_BASE` 指向部署域名。

## 重要：验证受限
自动化 curl 探测线上接口返回 **401（无 token）** / **302（SSO 登录跳转）**，
这是 EdgeOne Makers 预览部署的鉴权行为，**不代表部署失败**。
必须在**系统浏览器**打开（内置浏览器无法处理 SSO 302）。

## 清理
根目录 6 个诊断垃圾文件（`_bom.txt`、`_diag.txt`、`_env_probe.txt`、
`_pwsh_probe.txt`、`_build_run.log`、`_build_run_utf8.txt`）
已归档到 `.workbuddy/tmp/diag-backup/`，未直接删除。

## 项目现状总览（9-20）
| 模块 | 状态 |
|---|---|
| `spec/` 协议层 | ✅ OTA v1 + OpenAPI 3.1 |
| `server/` FastAPI | ✅ 19/19 冒烟 |
| `clients/manju_flutter/` | ✅ analyze 0/0、7/7 测试；含 android 原生 + ohos |
| `clients/manju_tauri/` | 🟡 骨架已建（Cargo/tauri.conf/src），未完整 |
| `scripts/` | ✅ gen_keypair、ota_manifest、smoke_test、build_all.ps1 |
| `deploy/free-server/` | ✅ Caddy + systemd 自建服务器方案 |
| `makers/` | ✅ **已部署上线** |
| `.github/workflows/deploy.yml` | ✅ CI |
| `docs/` | 🟡 仅 ota-integration.md |

## 「全部补齐」这轮的进展
- ✅ `docs/build-release.md` 已写：构建全流程、各平台**签名要求对照表**、
  OTA 发布、灰度放量、EdgeOne / 自建服务器双部署路线、发布检查清单
- ✅ `docs/ci-setup.md` 已写：Secrets 清单（EDGEONE_API_TOKEN 等 10 项）、
  构建矩阵示例、自托管 macOS runner、release 流水线（含 iOS/鸿蒙必须带 --store-url 的红线）
- 🔄 `clients/manju_rn/` — 派 rn-engineer 从零创建（RN 双端 + harmony/ ArkTS）
- 🔄 `clients/manju_tauri/` — 派 tauri-finisher 补齐业务代码
  （骨架已有：index.html/package.json/vite.config/src-tauri 三件套）

## 关键事实：签名是自动安装的前提
| 平台 | 签名 |
|---|---|
| macOS | **必须** Developer ID + 公证，否则 Gatekeeper 拦截 |
| Android | **必须** keystore，换 keystore = 换应用 |
| iOS / 鸿蒙 | **必须** 平台证书 + Profile，只能商店分发 |
| Windows | MSIX 建议签名（免管理员权限，自更新体验最好） |
| Linux | 通常不需要 |

## 补齐完成（两路工程师交付已验证）
- ✅ `clients/manju_tauri/` — 31 文件
  - 前端 screens/api/styles、updater（engine/install/verify/download/device/publicKey/UpdaterModal/useUpdater）
  - Rust: src-tauri/src/{main,lib}.rs、capabilities/default.json
  - 更新器**双路径**：A 官方 updater plugin（需签名）/ B 自定义下载+校验+拉起安装器（默认）
  - policy 分流：silent/forced/suggest 均已落地
  - 遗留：macOS 未签名会被 Gatekeeper 拦截；icons/* 需自行补全
- ✅ `clients/manju_rn/` — 49 文件（裸 RN 0.74 + TS，非 Expo）
  - Android: UpdaterModule.kt（PackageInstaller）+ Manifest 含 REQUEST_INSTALL_PACKAGES + FileProvider
  - iOS: UpdaterModule.swift 仅商店跳转，禁写安装
  - harmony/: 独立 ArkTS 工程（Index/Updater/EntryAbility/module.json5）
  - 红线拦截在 `src/updater/index.tsx:161` → `if (!canSelfInstall(platform))` 直接转 store_fallback
  - 遗留：未 npm install；iOS 需 Xcode 设 Bridging Header；Android 需 `gradle wrapper` 生成 gradlew

## 关键教训：工程师说"完成"不等于能编译
本轮**亲自跑构建**，抓出两处工程师漏掉的真实问题：

1. **Tauri 端 `npm run build` 19 处 TS 错误**（tauri-finisher 报告"完成"但没跑构建）
   - `useUpdater.ts` 含 JSX 却是 .ts 扩展名 → 已重命名为 .tsx（我修的）
   - `getBuildNumber` 不是 `@tauri-apps/api/app` 的导出
   - `deviceId` 应为 `device_id`（OTA 协议是 snake_case）
   - `downloadAndInstall` 不是 plugin-updater 模块级导出，应先 `check()` 再在 update 对象上调
   - `appCacheDir`/`tempDir` 不是 plugin-os 的导出
   - `@noble/hashes/utils` 没有 `base64` 导出（应为 `bytesToBase64`）
   - 已把这些整理成清单 SendMessage 让 tauri-finisher 返工

2. **RN 端 `npm install` 直接失败**：`react-native@0.74.5` 与
   `@react-native/babel-preset@0.74.5` **版本不存在**；
   `metro-react-native-babel-preset` 已废弃。
   实际最新是 **0.87.1**，且 peer 要求 `react ^19.2.3` / `@types/react ^19.1.1`。
   已改写 package.json（react 19.2.3、navigation 7.x、移除废弃包与 @types/react-native）。

## 本轮我自己做的
- ✅ Android keystore：`clients/manju_rn/android/app/manju-release.keystore`
  （keytool，RSA 2048 / 10000 天）+ `android/keystore.properties`
- ✅ Tauri 图标：`scripts/gen_icons.py`（**纯标准库**手写 PNG/ICO/ICNS，
  零第三方依赖，因为 pip 镜像拉不到 Pillow）→ `src-tauri/icons/` 6 个文件
- ✅ `.gitignore` 补签名物料排除（`*.keystore`、`*.jks`、`keystore.properties`、
  `*.mobileprovision`、`*.p7b` 等）—— 原本漏了，有泄露风险
- ✅ `docs/signing-setup.md`：Android keystore 用法 + Gradle 接入（Groovy/Kotlin 双写法）、
  macOS codesign + notarytool + stapler 完整命令、Windows 自签名与正式证书、
  Linux GPG、EdgeOne 绑定域名步骤、图标说明

## 仍待办
- 线上永久域名需在 EdgeOne 控制台绑定自定义域名（预览链接带 token 且 3 小时过期）
- Apple Developer ID 证书 + macOS 公证需用户本人在 Xcode 操作（我无法代办）
- 四端均未在真机跑通（本机无 Android SDK / Xcode / Rust / DevEco）

---

# 同日续：真跑验证 + 图标全端补齐 + 私钥泄露

## 验证结果（全部自己跑，非听汇报）
| 项 | 命令 | 结果 |
|---|---|---|
| Tauri 前端 | `npm run build`（manju_tauri） | ✅ 退出码 0，dist 产出（815KB JS / 4.65KB CSS） |
| RN 类型 | `npx tsc --noEmit`（manju_rn） | ✅ 退出码 0（修完 15 处错误后） |
| 后端冒烟 | `python scripts/smoke_test.py` | ✅ 19/19 |

### 后端启动方式（重要，下次直接用）
本机只有 `C:/Users/addk1/.workbuddy/binaries/python/envs/manju/Scripts/python.exe`
装了 uvicorn/fastapi。**其他 python 解释器都没有**，别再试错。
```
cd server && <manju-python> -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```
注意：健康检查路径是 `/healthz` 不是 `/health`；用 `&` 起的进程会随 bash 调用结束被杀，
要用 `run_in_background: true` 才能常驻。

### RN 15 处 TS 错误（我自己修的，rn-engineer 撞 429）
- `device.ts`：`DeviceInfo.getArchitecture()` / `getSupportedAbis()` 都不存在，
  正确 API 是 **`DeviceInfo.supportedAbis()`**（小写驼峰，返回 Promise<string[]>）
- `updater/index.tsx` 5 处 failed 进度缺 `ratio` → 补 `ratio: 0`
- `updater/index.tsx:322`：`useRef<Updater>()` → `useRef<Updater | null>(null)`（React 19 类型要求初值）
- `verify.ts`：`RNFS.readStream` 不在类型里 → 改成 `RNFS.stat` + 循环 `RNFS.read(path,len,pos,'base64')`
- `SearchScreen.tsx:80`：`ListEmptyComponent` 不接受 null，改 undefined
- `SettingsScreen.tsx`：删未使用的 DeviceInfo import

## 🔴 发现的安全事故：OTA 私钥已推送到 GitHub
- `keys/ota_private.pem` 被 commit `988d152 init: proj-07` 跟踪
- 远端 `origin/main`（https://github.com/afdk1991/manju）**确认含该文件**
- `.gitignore` 虽已写 `keys/ota_private.pem`，但**对已跟踪文件无效**
- 已执行 `git rm --cached keys/ota_private.pem`（文件仍在本地磁盘）
- ⚠️ **git 历史里还在**，需 filter-repo 清理 + 轮换密钥

## 图标缺口：不止 Tauri，四端都缺（已全部补齐）
新脚本 `scripts/gen_icons_all.py`（零依赖，复用 gen_icons.py 的 encode_png/render）：
- Flutter Windows `windows/runner/resources/app_icon.ico`（6 尺寸）→ 缺失会导致 MSVC 资源编译失败
- Flutter Linux `linux/icon.png`
- Flutter ohos + RN harmony：`base/media/` 的 background/foreground/startIcon/app_icon
- RN Android：`res/mipmap-{mdpi..xxxhdpi}/ic_launcher.png` 五档 → 缺失 Gradle 直接报错

**比图标更致命**：鸿蒙两端的 `base/element/string.json`、`element/color.json`、
`media/layered_image.json` **整个目录都不存在**，而 `module.json5` 里引用了
`$media:layered_image`、`$string:EntryAbility_label`、`$color:start_window_background`
——DevEco 必然编译失败。已补齐。

## ✅ 密钥轮换已完成（用户选择"轮换 + 清历史"）
新公钥 `f2obZdLFwhWJtuA/u/VW/EB1jAZ/UiZF/eEbxFJ0a3c=`，旧公钥 `DuW8zxjU...` 作废。
同步更新了 5 处：flutter ota_public_key.dart、rn publicKey.ts、tauri publicKey.ts、
**tauri.conf.json 的 plugins.updater.pubkey**（容易漏）、makers 两份 ota/public-key.json。
密码学验证：签名 64B、新公钥验签通过、篡改被拒、**旧公钥验新签名被拒**。
历史清除：`git filter-repo --path keys/ota_private.pem --invert-paths --force`
→ 旧 blob a842bff 已不可读、rev-list 全对象命中数 0、commit 数仍为 10。
filter-repo 会**删掉 origin 远端**，需重新 `git remote add` 再 `push --force`。
强推已成功，远端 `origin/main` 现只含 ota_public.pem。
轮换后重启服务端，冒烟仍 19/19，服务端下发的公钥已是新值。
**用户待办**：更新 GitHub Secret `ED25519_PRIVATE_KEY` 与 EdgeOne `MANJU_OTA_PRIVATE_KEY`。

## 阻塞项清单 docs/blockers.md（用户要"已实现/已验证/待真机验证"严格区分）
产出前**实际核查**而非凭记忆，挖出 6 个此前没记录的问题：

1. 🔴 **RN Android `app/debug.keystore` 根本不存在** → debug/release 构建都会失败。
   已用 keytool 生成（alias androiddebugkey / 密码 android / CN=Android Debug）。
   RN release 原本 `signingConfig signingConfigs.debug` → 已改为读 keystore.properties。
2. 🔴 **Flutter Android release 也用 debug 签名**（build.gradle.kts:36）
   → 已加 release signingConfig，缺文件时 throw GradleException（不再静默降级）。
   Flutter 复用 RN 那把 manju-release.keystore 的副本，保证发布主体一致。
3. 🔴 **Tauri capabilities 的 http:allow-fetch 只允许 api/cdn.example.com**
   → 真实域名请求会被 Tauri 权限系统直接拦截。已改为真实域名 + localhost。
4. 🔴 **Tauri updater.endpoints 是 cdn.example.com 占位**，且官方插件验签对象是
   "文件字节"，与 OTA v1 的"规范化消息"签名**不兼容** → 已清空，不走假接通。
   自动更新仍走自研路径 B。
5. 🟡 Flutter ohos 的 Index.ets / UpdaterPlugin.ets 硬编码 localhost:8000，真机连不上
6. 🟡 Flutter macOS CODE_SIGN_STYLE Automatic(489)/Manual(504) 混用

### 环境探测结论（决定哪些能验证）
有：Java 21、Node 22.22.2。缺：Gradle、Android SDK、adb、Rust/cargo、
Flutter/Dart（曾装过被清理）、Xcode（Windows 不可能）、DevEco/hvigorw。
→ **四端打包链路在本机全部不可执行**。

### 图标复核纠正了自己的一个误判
Tauri `icons/icon.ico` 实际只有 **256 单档**（六档的是 Flutter Windows 那个）。
已给 gen_icons_all.py 加 `wrap_ico_multi` / `wrap_icns_multi`，
Tauri icon.ico 补为 16-256 六档、icon.icns 补 ic09(512)（原仅 ic08）。
ICNS 仍无 1024 条目，上 Mac App Store 前需重做。

### shell 环境坑
中途 bash 的 PATH 被破坏（ls/head 全 command not found），
解法：每条命令前 `export PATH="/usr/bin:/bin:$PATH"`。
服务端 python 仍只有 `envs/manju` 装了 uvicorn。

## 🟡 遗留：鸿蒙端 ArkTS 未做签名校验
`manju_flutter/ohos/.../UpdaterPlugin.ets` 与 `manju_rn/harmony/.../Updater.ets`
只有 `signature?: string` 字段声明，**没有 SHA256 / Ed25519 验签实现**。
因鸿蒙无静默安装 API、只能跳 AppGallery，风险限于"跳转 URL 被篡改"，靠 HTTPS 缓解。
本机无 DevEco 无法验证 cryptoFramework 的 Ed25519 支持，故未擅自实现。
